/** Global configuration, may be use vscode configuration in future. */
export const PluginConfig = {

    /** All supported tagged name. */
    tags: ['html', 'css', 'svg'],

    /** Plugin name. */
    pluginName: 'lupos-server',

    /** Get replaced to false after publishing. */
    debugging: true,
}